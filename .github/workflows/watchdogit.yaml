name: sample-workflow
on: 
    push:
        branches:
            - main
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: get code #download the code
              uses: actions/checkout@v2